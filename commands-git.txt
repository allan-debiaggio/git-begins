git clone <URL> 
git push <remote> <branch>
git push <remote> --force
git push <remote> --tags
git pull <remote>
git checkout -b <nom de la branche> 		Créer et se déplacer dans la nouvelle branche
git checkout <nom de la branche>		Se déplacer dans une autre branche